package models.factory;

public class Board extends AComponent {
    @Override
    public String toString() {
        return super.toString();
    }

    Board(String identifier) {
        super(identifier);
    }
}
