package models.factory;

public class Card extends AComponent{
    Card(String identifier) {
        super(identifier);
    }

    @Override
    public String toString() {
        return super.toString();
    }
}
