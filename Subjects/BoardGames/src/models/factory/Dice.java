package models.factory;

public class Dice extends AComponent{
    Dice(String identifier) {
        super(identifier);
    }

    @Override
    public String toString() {
        return super.toString();
    }
}
