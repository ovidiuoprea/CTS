package models.factory;

public class Token extends AComponent{
    Token(String identifier) {
        super(identifier);
    }

    @Override
    public String toString() {
        return super.toString();
    }
}
